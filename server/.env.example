# Copy to .env and fill in. The server runs without these (mock DM mode).

# Set to enable the real AI Dungeon Master (Claude). Without it, a built-in
# mock narrator is used so the game is still fully playable.
ANTHROPIC_API_KEY=

# Port for the HTTP + WebSocket server (default 8080).
PORT=8080
